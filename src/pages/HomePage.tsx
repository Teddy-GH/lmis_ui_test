/* eslint-disable @typescript-eslint/no-unused-vars */

import { Form, List } from 'antd';

const HomePage = () => {
  return (
    <main className="flex flex-col gap-4 bg-blue-700">
      <Form />
      <List />
    </main>
  );
};

export default HomePage;
